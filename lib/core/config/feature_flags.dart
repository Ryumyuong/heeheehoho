/// 스토어 출시 전까지 켜고 끄는 기능 스위치.
///
/// 준비가 덜 된 기능을 코드째 지우지 않고 화면에서만 감추기 위한 곳이다.
/// (마켓 잠금은 화면에 붙어 있는 `store_page.dart`의 `kMarketReady` 참고)
library;

/// 소셜 로그인(구글·카카오·네이버) 버튼을 보여줄지.
///
/// **지금은 실제 인증이 붙어 있지 않다** — 버튼을 눌러도 `isGuest`만 바꾸고
/// 홈으로 갈 뿐이다. 동작하지 않는 로그인은 애플·구글 심사 거절 사유라서,
/// 실제 연동(firebase_auth 등)이 끝날 때까지 버튼을 감춘다.
///
/// 연동을 마치면 이 값을 true로 바꾸면 버튼이 다시 나온다. iOS에 소셜 로그인을
/// 노출하는 순간 Sign in with Apple도 함께 넣어야 심사를 통과한다.
const bool kSocialLoginEnabled = false;

/// 뼈다귀 지갑 칩을 보여줄지.
///
/// 뼈다귀는 마켓(실물 상품·기부)에서만 쓰는 화폐인데 마켓이 닫혀 있어
/// 지금은 쓸 곳도 얻을 곳도 없다. 마켓을 열 때 `kMarketReady`와 함께 켤 것.
const bool kBonesEnabled = false;
